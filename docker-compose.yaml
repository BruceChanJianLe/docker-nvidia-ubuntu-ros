version: "28.0.4"

services:
  u20:
    build:
      context: .
      args:
        - user_id=$(id -u)
    ports:
      host
    environment:
      - DISPLAY=$DISPLAY
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /etc/localtime:/etc/localtime:ro
      - /dev:/dev
      - /media:/media
